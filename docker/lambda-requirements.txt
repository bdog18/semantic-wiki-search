# Lambda serving set: the App Runner list plus Mangum, minus boto3 (the
# Lambda base image already ships it) -- artifacts.py imports boto3 lazily
# and only for s3:// paths, which this image doesn't use since the index is
# baked in.
faiss-cpu==1.15.0
sentence-transformers==5.6.1
numpy==2.5.1
tqdm==4.67.1
fastapi==0.141.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
mangum==0.19.0
